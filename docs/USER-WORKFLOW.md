# Руководство пользователя: Полный путь от архивов до готовых архивов

Пошаговое описание процесса от распаковки исходных архивов локализации до получения обработанных архивов.

---

## 📂 Структура папок

```
localization/
├── input/                    # Входные данные (распакованные архивы)
│   └── business50/           # Папка проекта (название любое)
│       ├── en/               # Содержимое file-en.tar.gz
│       ├── ru/               # Содержимое file-ru.tar.gz
│       ├── ua/               # Содержимое file-ua.tar.gz (если есть)
│       └── localization.json # Создаётся на шаге 2
│
└── output/                   # Результаты (восстановленные файлы + архивы)
    └── business50-restored/
        ├── en/               # Восстановленные PHP-файлы
        ├── ru/
        ├── ua/
        ├── file-en.tar.gz    # Готовые архивы (шаг 5)
        ├── file-ru.tar.gz
        ├── file-ua.tar.gz
        └── ...
```

---

## 🔄 Шаг 1: Подготовка входной папки

Создайте папку для проекта и распакуйте архивы локализации Bitrix24.

```bash
# Создать папку проекта
mkdir -p input/business50
cd input/business50

# Распаковать архивы (каждый архив создаёт папку с кодом языка)
tar -xzf /path/to/file-en.tar.gz
tar -xzf /path/to/file-ru.tar.gz
tar -xzf /path/to/file-ua.tar.gz
tar -xzf /path/to/file-tr.tar.gz
# ... другие языки по необходимости
```

**Важно:** Каждый архив должен распаковаться в папку с кодом языка (`en/`, `ru/`, `ua/`, `tr/`).  
Обычно `file-en.tar.gz` содержит папку `en/` — при распаковке в `input/business50/` получится `input/business50/en/`.

**Проверка структуры:**
```bash
ls input/business50/
# Должны быть: en  ru  ua  tr  (и другие языки)
```

---

## 📋 Шаг 2: Агрегация (сбор в JSON)

Скрипт сканирует папки языков и создаёт единый `localization.json` со всеми переводами.

```bash
pnpm aggregate input/business50
```

**Результат:** `input/business50/localization.json`

Файл содержит все ключи локализации в формате:
```json
{
  "module/lang/{lang}/file.php": {
    "KEY_NAME": {
      "ru": "Значение",
      "en": null,
      "ua": null
    }
  }
}
```

---

## 🌍 Шаг 3: Перевод

Заполнение отсутствующих переводов (значения `null`).

### Вариант A: Локальная модель (Qwen3-8B, Apple Silicon)

```bash
./run_translation.sh input/business50/localization.json
```

- Автоматически запускает MLX-сервер
- Сохраняет результат в тот же файл (in-place)
- Можно прервать Ctrl+C — прогресс сохранится, запуск повторно продолжит с места остановки

### Вариант B: API (OpenAI или совместимый)

```bash
TRANSLATION_BACKEND=api pnpm translate input/business50/localization.json \
  --required=ru,tr,en \
  --output=.
```

Настройте `.env`:
```ini
TRANSLATION_BACKEND=api
OPENAI_API_KEY=sk-proj-....
```

**Результат:** `localization.json` обновлён — заполнены переводы для указанных языков.

---

## 📤 Шаг 4: Восстановление в файловую структуру

Разворачивает JSON обратно в PHP-файлы.

```bash
pnpm restore input/business50/localization.json output/business50-restored
```

**Результат:** В `output/business50-restored/` создаётся структура:
```
output/business50-restored/
├── en/
│   └── module/
│       └── lang/
│           └── en/
│               └── file.php
├── ru/
│   └── ...
├── ua/
│   └── ...
└── tr/
    └── ...
```

---

## 📦 Шаг 5: Создание архивов

Упаковка обработанных файлов в архивы для развёртывания.

```bash
pnpm create-archives output/business50-restored
```

**Результат:** В `output/business50-restored/` создаются:
- `file-en.tar.gz`
- `file-ru.tar.gz`
- `file-ua.tar.gz`
- `file-tr.tar.gz`
- ... (по одному на каждый язык)

Готовые архивы можно использовать для обновления Bitrix24.

---

## 📊 Сводная схема

```
┌─────────────────────────────────────────────────────────────────┐
│  ШАГ 1: Распаковка                                               │
│  file-en.tar.gz, file-ru.tar.gz, ...  →  input/business50/en/,  │
│                                           input/business50/ru/   │
└─────────────────────────────────────────────────────────────────┘
                                    ↓
┌─────────────────────────────────────────────────────────────────┐
│  ШАГ 2: Агрегация                                                │
│  pnpm aggregate input/business50                                │
│  → input/business50/localization.json                           │
└─────────────────────────────────────────────────────────────────┘
                                    ↓
┌─────────────────────────────────────────────────────────────────┐
│  ШАГ 3: Перевод                                                 │
│  ./run_translation.sh input/business50/localization.json         │
│  (или pnpm translate с TRANSLATION_BACKEND=api)                 │
│  → localization.json обновлён in-place                         │
└─────────────────────────────────────────────────────────────────┘
                                    ↓
┌─────────────────────────────────────────────────────────────────┐
│  ШАГ 4: Восстановление                                           │
│  pnpm restore input/business50/localization.json \               │
│    output/business50-restored                                   │
│  → output/business50-restored/en/, ru/, ua/, tr/               │
└─────────────────────────────────────────────────────────────────┘
                                    ↓
┌─────────────────────────────────────────────────────────────────┐
│  ШАГ 5: Создание архивов                                        │
│  pnpm create-archives output/business50-restored                 │
│  → file-en.tar.gz, file-ru.tar.gz, ... в той же папке           │
└─────────────────────────────────────────────────────────────────┘
```

---

## 🛠 Опциональные шаги

### fill-gaps (заполнение из существующих переводов)

Если одинаковый текст уже переведён в другом ключе — можно скопировать перевод:

```bash
pnpm fill-gaps input/business50/localization.json output/business50-filled
```

### Валидация

```bash
pnpm exec tsx scripts/validate_json.ts input/business50/localization.json
```

---

## ⚙️ Конфигурация

### Языки перевода

По умолчанию: `ru`, `tr`, `en`. Переопределить:

```bash
REQUIRED_LANGS=ru,en,ua,tr ./run_translation.sh input/business50/localization.json
```

### Backend перевода

- `local-server` — локальная модель Qwen (run_translation.sh)
- `api` — OpenAI или совместимый API

В `.env`:
```ini
TRANSLATION_BACKEND=local-server
# или
TRANSLATION_BACKEND=api
OPENAI_API_KEY=sk-...
```

---

## 📁 Пример полного запуска

```bash
# 1. Распаковка
mkdir -p input/business50
cd input/business50
tar -xzf ~/downloads/file-en.tar.gz
tar -xzf ~/downloads/file-ru.tar.gz
cd ../..

# 2. Агрегация
pnpm aggregate input/business50

# 3. Перевод (локальная модель)
./run_translation.sh input/business50/localization.json

# 4. Восстановление
pnpm restore input/business50/localization.json output/business50-restored

# 5. Архивы
pnpm create-archives output/business50-restored

# Готово! Архивы в output/business50-restored/
ls output/business50-restored/*.tar.gz
```
