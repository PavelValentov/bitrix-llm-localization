# Troubleshooting Guide

## Performance Issues

### Проблема: "Не вижу увеличения производительности при BATCH_SIZE=10"

#### Возможные причины:

### 1. ✅ ИСПРАВЛЕНО: Режим размышлений был случайно включён

**Проблема:** `z.coerce.boolean()` в Zod неправильно парсил строку `"false"` как `true`.

```typescript
// ❌ Было (неправильно):
ENABLE_MODEL_THINKING: z.coerce.boolean().default(false)
// Строка "false" → true (любая непустая строка = true в JS)

// ✅ Стало (правильно):
ENABLE_MODEL_THINKING: z.string().optional().default('false').transform(val => val === 'true')
// Строка "false" → false
// Строка "true" → true
```

**Как проверить:**
```bash
node test-config.mjs
```

Должно показать:
```
Enable Thinking: false  ← Должно быть false!
```

Если показывает `true`, значит проблема не исправлена.

**Влияние на производительность:**
- С размышлениями: ~50 строк/минуту
- Без размышлений: ~150 строк/минуту
- **Разница: 3x медленнее!**

---

### 2. Модель тратит больше времени на большие батчи

**Факт:** MLX-модели линейно увеличивают время обработки.

```
Batch Size 1:  ~3 секунды на батч  = 20 строк/минуту
Batch Size 5:  ~4 секунды на батч  = 75 строк/минуту
Batch Size 10: ~6 секунд на батч   = 100 строк/минуту
Batch Size 20: ~12 секунд на батч  = 100 строк/минуту
```

**Почему не 10x быстрее?**

1. **Генерация токенов** - модель генерирует больше токенов (JSON структура)
2. **Контекст** - больше контекста для каждой строки
3. **Качество** - модель тратит время на анализ всех строк сразу

**Оптимальное значение: `BATCH_SIZE=5-10`**
- Выше 10: время растёт быстрее, чем количество строк
- Ниже 5: слишком много запросов

---

### 3. ✅ ИСПРАВЛЕНО: Неправильный дефолт `MAX_RESPONSE_TOKENS`

**Проблема:** Дефолтное значение было `4096` вместо `16384`.

```typescript
// ❌ Было:
MAX_RESPONSE_TOKENS: z.coerce.number().int().positive().default(4096)

// ✅ Стало:
MAX_RESPONSE_TOKENS: z.coerce.number().int().positive().default(16384)
```

**Влияние:**
- Для больших батчей (10-20 строк) ответ модели мог обрезаться
- JSON parsing errors при неполном ответе
- Повторные попытки замедляют процесс

---

## Как правильно измерить производительность

### Создайте тестовый файл с 50 строками:

```bash
# Скопируйте 50 строк из localization.json
head -100 input/business50/localization.json > test-50-strings.json
```

### Запустите с разными настройками:

```bash
# Тест 1: Batch Size 1
TRANSLATION_BATCH_SIZE=1 time ./run_translation.sh test-50-strings.json

# Тест 2: Batch Size 5
TRANSLATION_BATCH_SIZE=5 time ./run_translation.sh test-50-strings.json

# Тест 3: Batch Size 10
TRANSLATION_BATCH_SIZE=10 time ./run_translation.sh test-50-strings.json
```

### Ожидаемые результаты:

```
Batch Size 1:  ~150 секунд (50 × 3s)   ← Медленно
Batch Size 5:  ~40 секунд  (10 × 4s)   ← Хорошо
Batch Size 10: ~30 секунд  (5 × 6s)    ← Лучше
```

---

## Checklist: Максимальная производительность

- [ ] ✅ `ENABLE_MODEL_THINKING=false` (проверить через `node test-config.mjs`)
- [ ] ✅ `TRANSLATION_BATCH_SIZE=10` (или 5-10)
- [ ] ✅ `MAX_RESPONSE_TOKENS=16384` (или 8192 для экономии памяти)
- [ ] ✅ `LOCAL_SERVER_RELOAD_EVERY_BATCHES=50` (предотвращает утечки памяти)
- [ ] ✅ Модель: `Qwen3-8B-MLX-8bit` (не Qwen3-VL!)
- [ ] ✅ MLX-сервер запущен и отвечает на `/health`

---

## Типичные ошибки

### Ошибка 1: "Translation too slow"

**Проверьте:**
```bash
node test-config.mjs | grep "Enable Thinking"
```

Если `true` - ВОТ ПРОБЛЕМА!

**Исправьте:**
```bash
# В .env
ENABLE_MODEL_THINKING=false
```

---

### Ошибка 2: "JSON parsing errors"

**Причина:** `MAX_RESPONSE_TOKENS` слишком маленький.

**Проверьте логи:**
```bash
tail -100 logs/translation.log | grep "Failed to parse"
```

**Исправьте:**
```bash
# В .env
MAX_RESPONSE_TOKENS=16384
```

---

### Ошибка 3: "Batch size не работает"

**Проверьте логи:**
```bash
tail -50 logs/translation.log | grep "Batch"
```

Должно показывать:
```
📤 Batch 1/2: 10 keys  ← Правильно (10 ключей в батче)
```

Если показывает:
```
📤 Batch 1/10: 1 keys  ← Неправильно (1 ключ в батче)
```

**Причина:** `.env` файл не загрузился или переменная перезаписана.

**Исправьте:**
```bash
# Убедитесь, что в .env только ОДНА строка:
TRANSLATION_BATCH_SIZE=10

# Удалите дубликаты!
```

---

## Инструменты для диагностики

### 1. Проверка конфигурации

```bash
node test-config.mjs
```

### 2. Проверка работы MLX-сервера

```bash
curl http://127.0.0.1:8765/health
# Должен вернуть: {"status":"ok","model_loaded":true}
```

### 3. Проверка скорости батча

```bash
# Запустите один батч и замерьте время
time curl -X POST http://127.0.0.1:8765/translate \
  -H "Content-Type: application/json" \
  -d '{"messages":[{"role":"system","content":"Translate"},{"role":"user","content":"Hello"}],"max_tokens":2048,"temperature":0.3,"enable_thinking":false}'
```

---

## Итоговые рекомендации

**Для максимальной производительности:**

```bash
# .env
TRANSLATION_BACKEND=local-server
LOCAL_SERVER_URL=http://127.0.0.1:8765
LOCAL_SERVER_RELOAD_EVERY_BATCHES=50

TRANSLATION_BATCH_SIZE=10
MAX_RESPONSE_TOKENS=8192
ENABLE_MODEL_THINKING=false

MLX_MODEL_PATH=~/.lmstudio/models/lmstudio-community/Qwen3-8B-MLX-8bit
```

**Ожидаемая производительность:**
- ~100-150 строк в минуту
- ~6,000-9,000 строк в час
- ~50,000-70,000 строк за 8 часов работы

**Для вашего файла (95,371 ключей):**
- При 100 строк/минуту: ~16 часов
- При 150 строк/минуту: ~10.5 часов

---

**Last Updated:** 2026-02-08
**Version:** 1.0.0
