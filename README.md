# Bitrix24 Localization Tools

Набор инструментов для автоматизации перевода и управления языковыми файлами Bitrix24. Позволяет агрегировать разрозненные файлы локализации в единый JSON, переводить их (в перспективе через LLM) и восстанавливать обратно в файловую структуру с полной симметрией языков.

## 🚀 Основные возможности

*   **Агрегация (`aggregate`)**: Сканирует директорию с модулями Bitrix24, находит все языковые файлы (`/lang/ru/`, `/lang/en/` и т.д.) и собирает их в единый `localization.json`. Поддерживает пустые файлы и обеспечивает нормализацию путей.
*   **Восстановление (`restore`)**: Разворачивает переводы из `localization.json` обратно в файловую структуру. Автоматически создает файлы для всех активных языков, обеспечивая полную симметрию (если файл есть в `en`, он будет создан и в `ru`, даже если пустой).
*   **LLM Перевод (`translate`)**: Автоматический перевод отсутствующих строк через OpenAI API (или совместимые модели).
*   **Валидация и тесты**: Встроенные инструменты для проверки целостности данных и отсутствия потерь.

## 📦 Установка

Для работы требуются [Node.js](https://nodejs.org/) (v18+) и [pnpm](https://pnpm.io/).

1.  Клонируйте репозиторий:
    ```bash
    git clone https://github.com/your-repo/bitrix-localization.git
    cd bitrix-localization
    ```

2.  Установите зависимости:
    ```bash
    pnpm install
    ```

3.  **Настройка окружения (.env)**
    Для работы скрипта перевода необходимо создать файл `.env` в корне проекта и добавить туда API ключ OpenAI (или совместимого сервиса).

    ```bash
    cp .env.example .env
    ```

    Откройте `.env` и укажите ваш ключ:
    ```ini
    OPENAI_API_KEY=sk-proj-....
    # Опционально: выберите модель (по умолчанию gpt-4o)
    # OPENAI_MODEL=gpt-4-turbo
    ```

## 🛠 Использование

### 1. Агрегация переводов (Сбор в JSON)

Скрипт сканирует указанную папку (например, выгрузку модулей Bitrix) и создает JSON файл со всеми найденными строками.

```bash
pnpm aggregate <input-dir> [output-file]
```

*   `<input-dir>`: Путь к папке с исходными файлами (например, `./business50`).
*   `[output-file]`: (Опционально) Путь к выходному файлу. По умолчанию создает `localization.json` внутри `<input-dir>`.

**Пример:**
```bash
# Соберет все переводы из папки business50 в файл business50/localization.json
pnpm aggregate business50
```

### 2. Автоматический перевод (LLM / OpenAI)

Запускает процесс перевода для ключей, отсутствующих в целевых языках через OpenAI API.

```bash
pnpm translate <json-file> [--required=en,ua] [--output=output]
```

### 3. Локальный Перевод (Python / NLLB)

Для перевода без использования внешних API (бесплатно, офлайн) используется локальная нейросеть NLLB-200.

**Быстрый запуск (Mac/Linux):**
Используйте скрипт `run_translation.sh` в корне проекта. Он автоматически создаст виртуальное окружение, установит зависимости и запустит перевод.

```bash
./run_translation.sh
```

Вы можете передать дополнительные аргументы (например, лимит для теста):
```bash
./run_translation.sh --limit 100
```

**Ручной запуск:**
1. Создайте и активируйте venv:
   ```bash
   python3 -m venv venv
   source venv/bin/activate
   ```
2. Установите зависимости:
   ```bash
   pip install -r python-translator/requirements.txt
   ```
3. Запустите скрипт:
   ```bash
   python python-translator/bitrix24_translator_complete.py --help
   ```

**Особенности NLLB переводчика:**
*   **Multi-Source Consensus**: Использует *все* доступные переводы (например, EN и RU) для генерации наилучшего перевода на целевой язык (например, TR).
*   **Умный выбор**: При переводе на украинский приоритет отдается русскому источнику, на немецкий — английскому.
*   **Защита макросов**: Специальная логика сохраняет переменные `#MACRO#` и HTML-теги.

### 4. Восстановление файлов (Развертывание)

Скрипт берет JSON и воссоздает полную структуру файлов в целевой папке.

```bash
pnpm restore <json-file> <output-dir>
```

*   `<json-file>`: Путь к файлу `localization.json`.
*   `<output-dir>`: Папка, куда будут сохранены восстановленные файлы (например, `./business100`).

**Пример:**
```bash
# Развернет переводы из JSON в папку business100
pnpm restore business50/localization.json business100
```

### 5. Тестирование

Запуск unit-тестов для проверки логики парсинга и генерации:

```bash
pnpm test
```

## 🖥 Совместимость (Windows, Linux, macOS)

Скрипты написаны на TypeScript и запускаются через `tsx` (Node.js), поэтому они полностью кроссплатформенны.

### Windows (PowerShell / CMD)
```powershell
# Установка
pnpm install

# Запуск (используйте обратные или прямые слеши, Node.js понимает оба)
pnpm aggregate C:\work\bitrix\modules
pnpm restore C:\work\bitrix\modules\localization.json C:\work\bitrix\export
```

### Linux / macOS
```bash
# Установка
pnpm install

# Запуск
pnpm aggregate ./modules
pnpm restore ./modules/localization.json ./export
```

## 📂 Структура проекта

*   `src/` — Исходный код логики (агрегатор, восстановитель, переводчик).
*   `scripts/` — CLI-обертки для запуска команд.
*   `tests/` — Тесты (Vitest).
*   `docs/` — Дополнительная документация.

## 🤝 Вклад в разработку

Если вы нашли ошибку в парсинге сложных PHP-файлов Bitrix, пожалуйста, добавьте проблемный кейс в `tests/utils.test.ts` и создайте Issue.
